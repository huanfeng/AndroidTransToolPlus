# pnpm 配置文件

# 使用严格的对等依赖
strict-peer-dependencies=false

# 提升模式（推荐）
shamefully-hoist=true

# 自动安装对等依赖
auto-install-peers=true

# 使用硬链接节省磁盘空间
link-workspace-packages=true

# 锁定文件版本
lockfile-version=9

# 镜像配置（可选，如果网络慢可以配置国内镜像）
# registry=https://registry.npmmirror.com
